#### Scale, Rotate then Translate Geometric Points with a Matrix

Create a matrix that can scale, rotate then translate geometry points.

Uses the [scale_rotate_translate_matrix](/api/physics/#scale-rotate-translate-matrix) function.
