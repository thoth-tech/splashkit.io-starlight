from splashkit import write_line, bin_to_dec

binary_value = "1111010"
decimal_value = bin_to_dec(binary_value)  # SplashKit function

write_line("Binary: " + binary_value)
write_line("Decimal: " + str(decimal_value))
