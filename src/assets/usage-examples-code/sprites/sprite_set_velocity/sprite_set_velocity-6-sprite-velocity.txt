### sprite_set_velocity() usage example

The following code is an example of using [sprite set velocity](https://splashkit.io/api/sprites/#sprite-set-velocity) which sets the velocity of a sprite on a splashkit window.