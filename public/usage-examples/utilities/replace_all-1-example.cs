using static SplashKitSDK.SplashKit;
// Replace all occurrences of 'foo' with 'bar' in a sentence
string s = "foo is not bar, but foo can be bar";
System.Console.WriteLine(ReplaceAll(s, "foo", "bar"));
