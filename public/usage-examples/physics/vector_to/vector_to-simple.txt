#### Create a Vector To Specified x and y Parameters

Returns a new vector using specific x and y value parameters. This is a vector from the origin to that point. Outputs the value of the vector to the terminal.

Uses the [vector_to](/api/physics/#vector-to-2) function.