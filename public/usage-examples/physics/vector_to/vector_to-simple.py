from splashkit import *

# Create a vector pointing to the specified coordinates
my_vector_1 = vector_to(200, 100)

# Output the vector
write_line("my_vector_1 values: " + vector_to_string(my_vector_1))
