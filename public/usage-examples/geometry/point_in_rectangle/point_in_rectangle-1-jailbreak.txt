#### Point In Rectangle

The following code shows examples of using [Point In Rectangle](/api/geometry#point-in-rectangle) to create a small cursor jail, where if the cursor leaves the police are called.